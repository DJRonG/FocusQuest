/* 🌊 FocusQuest Theme - Ocean meets Ancient Gold */

body {
  font-family: 'Segoe UI', sans-serif;
  background: linear-gradient(to bottom, #00264d, #006699);
  color: #f5deb3;
  margin: 0;
  padding: 0;
}

.container {
  max-width: 900px;
  margin: 40px auto;
  background: rgba(0, 0, 0, 0.4);
  border-radius: 16px;
  padding: 20px;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
}

header h1 {
  text-align: center;
  color: #ffcc33;
  font-size: 2em;
  margin-bottom: 5px;
}

header p {
  text-align: center;
  color: #e0e0e0;
  margin-bottom: 20px;
}

main {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

section {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 12px;
  padding: 15px;
}

textarea, input {
  width: 100%;
  background: rgba(255, 255, 255, 0.2);
  border: none;
  color: #fff;
  padding: 10px;
  border-radius: 8px;
  margin-top: 8px;
  font-size: 1em;
}

button {
  background-color: #ffcc33;
  color: #00264d;
  border: none;
  border-radius: 8px;
  padding: 10px 16px;
  margin-top: 10px;
  cursor: pointer;
  font-weight: bold;
}

button:hover {
  background-color: #ffdd55;
}

.score {
  grid-column: span 2;
  text-align: center;
  font-size: 1.2em;
}
